<%= form_for(@categoria_post) do |f| %>
  <% if @categoria_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categoria_post.errors.count, "error") %> prohibited this categoria_post from being saved:</h2>

      <ul>
      <% @categoria_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
